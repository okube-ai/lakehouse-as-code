secret-scope-azure:
  name: azure
  secrets:
    - key: keyvault-url
      string_value: https://o3-kv-lakehouse-${vars.env}.vault.azure.net/
    - key: tenant-id
      string_value: ${vars.azure_tenant_id}
    - key: client-id
      string_value: ${vars.azure_client_id}
    - key: client-secret
      string_value: ${vars.azure_client_secret}
secret-scope-databricks:
  name: databricks
  secrets:
    - key: host
      string_value: ${vars.DATABRICKS_HOST}
    - key: token-osoucy
      string_value: ${vars.DATABRICKS_TOKEN_OSOUCY}
  permissions:
    - permission: READ
      principal: role-metastore-admins
    - permission: READ
      principal: role-workspace-admins
