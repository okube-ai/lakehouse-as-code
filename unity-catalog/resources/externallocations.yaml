external-location-landing:
    name: external-location-landing-${vars.env}
    credential_name: ${resources.storage-credentials-lakehouse.name}
    force_destroy: True
    url: abfss://landing@o3stglakehouse${vars.env}.dfs.core.windows.net/
    grants:
      - principal: role-metastore-admins
        privileges:
          - ALL_PRIVILEGES
          - MANAGE
          - EXTERNAL_USE_LOCATION

external-location-metastore:
    name: external-location-metastore-${vars.env}
    credential_name: ${resources.storage-credentials-lakehouse.name}
    force_destroy: True
    url: abfss://metastore@o3stglakehouse${vars.env}.dfs.core.windows.net/
    grants:
      - principal: role-metastore-admins
        privileges:
          - ALL_PRIVILEGES
          - MANAGE
          - EXTERNAL_USE_LOCATION

external-location-tables:
    name: external-location-tables-${vars.env}
    credential_name: ${resources.storage-credentials-lakehouse.name}
    force_destroy: True
    url: abfss://tables@o3stglakehouse${vars.env}.dfs.core.windows.net/
    grants:
      - principal: role-metastore-admins
        privileges:
          - ALL_PRIVILEGES
          - MANAGE
          - EXTERNAL_USE_LOCATION


#
#external-location-landing-prd:
#    name: external-location-landing-prd
#    credential_name: ${resources.metastore-data-access-lakehouse-prd.name}
#    force_destroy: True
#    url: ${vars.landing_storage_location_prd}
#    grants:
#      - principal: role-metastore-admins
#        privileges:
#          - ALL_PRIVILEGES
#          - MANAGE
#          - EXTERNAL_USE_LOCATION
#    options:
#        provider: ${resources.provider-databricks-prd}
#
#
#external-location-tables-prd:
#    name: external-location-tables-prd
#    credential_name: ${resources.metastore-data-access-lakehouse-prd.name}
#    force_destroy: True
#    url: ${vars.tables_storage_location_prd}
#    grants:
#      - principal: role-metastore-admins
#        privileges:
#          - ALL_PRIVILEGES
#          - MANAGE
#          - EXTERNAL_USE_LOCATION
#    options:
#        provider: ${resources.provider-databricks-prd}
#
#external-location-metastore-prd:
#    name: external-location-metastore-prd
#    credential_name: ${metastore-data-access-lakehouse-prd.name}
#    force_destroy: True
#    url: ${vars.metastore_prd_storage_location}
#    grants:
#      - principal: role-metastore-admins
#        privileges:
#          - ALL_PRIVILEGES
#          - MANAGE
#          - EXTERNAL_USE_LOCATION
#    options:
#        provider: ${resources.provider-databricks-prd}
