- name: azure

  secrets:
    - key: keyvault-url
    - key: tenant-id
    - key: client-id
    - key: client-secret

  permissions:
    - permission: READ
      principal: role-metastore-admins
    - permission: READ
      principal: role-workspace-admins
